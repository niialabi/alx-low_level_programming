#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
  */
int main(void)
{
	char puzz[] = "\"Programming is like building a multilingual puzzle";

	puts(puzz);
	return (0);
}
