#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - Check string length function
 * @s: pointer to string
 * Description: checks and returns the lengh of a sting
 *
 * Return: returns and integer (Success)
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
