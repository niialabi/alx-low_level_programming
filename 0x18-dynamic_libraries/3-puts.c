#include "main.h"
#include <stdio.h>
/**
 * _puts - Main function pr-o to stoupt
 * @str: dereference pointer to string
 *
 * Description: program simply prints out to standard output
 */
void _puts(char *str)
{
	puts(str);
}
