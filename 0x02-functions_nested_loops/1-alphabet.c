#include "main.h"

/**
 * main - Entry point
 * printing small letter alphabets
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	void print_alphabet(void)
	{
		for (i = a; i < z; i++)
			_putchar (i);
	}
	_putchar ('\n');
	return (0);
}
